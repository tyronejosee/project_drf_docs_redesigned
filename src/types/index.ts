export type Tutorial = {
  title: string;
  path: string;
};

export type ApiGuide = {
  title: string;
  path: string;
};

export type Topic = {
  title: string;
  path: string;
};

export type Community = {
  title: string;
  path: string;
};

export type Heading = {
  id: string;
  text: string;
  depth: number;
};
